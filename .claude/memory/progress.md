# Work-log

> Append-only, ordine cronologico inverso.

## 2026-07-28 — Ciclo Cybersec endpoint governance (`5ca2dd6`) e chiusura fuga via nomi file (`21e11b3`)

Selezione di 9 documenti: `_Bitdefender (endpoint security)` per intero (6) piu' tre di governance non sensibili estratti da `_ GDPR E ISO27001` (politica PSGSI, template ISO27001, brief DORA). Esclusi a monte, dopo ispezione dei nomi: NDA intestato a persona nominata, due deleghe con nomi propri nel filename, un file su dati sanitari e donazione riferito a una persona (art. 9, categoria particolare), i report di rischio di due fornitori sull'infrastruttura reale con domini e IP, i questionari fornitori (126 file, dati commerciali di terzi) e la polizza assicurativa. Due file con nomi che fuori dal loro albero perdevano significato (`09092025.docx`, `primo accesso.docx`) prefissati col tema della sottocartella.

Pre-flight `prepare_graphify_source.py`: 9 su 9 passano, il filtro di graphify non morde su questo corpus. graphify su `account1` (non `account2` come a luglio, perche' il launcher e' stato invocato senza `-Account`; nota: quella sessione ha segnalato login in scadenza in 3 giorni). Grafo: 35 nodi, 53 link, 7 community, 68% EXTRACTED / 32% INFERRED. God node principale: SGSI/ISMS con 9 archi.

**Collaudo del fix di routing: superato.** L'automatismo ha mandato 23 fit su 24 a `Cybersecurity & IT Governance` e zero a `Quality Certification`. Review manuale: 5 correzioni su 28 contro 18 su 34 del ciclo precedente. Le 5 erano per una causa diversa dal GDPR: token generici (`IP`, `firewall`) che tirano i nodi endpoint verso `Networking Engineering & Security`, e `SGSI` finito in `IT Administration & Billing`. Tenuta su Networking solo `Problema connessione RDP LAN`, corretta. Tutti i new_domain scartati come da prassi.

**Tre difetti scoperti e corretti in corsa.** (a) In `extract_entities.py` il confronto fra token hostname e `TECH_BRAND_STOPWORDS` era case-sensitive mentre le regex hostname matchano solo maiuscolo: il filtro non scattava mai, e `WINDOWS` diventava `[HOSTNAME_1]` in un corpus dove la parola e' ubiqua. Fix: confronto su set minuscolo precalcolato. (b) Il NER spaCy produceva span a cavallo di interruzioni di riga, cosi' `Bitdefender Gravityzone` piu' tre righe di residui di template diventava `[PERSONA_5]`, mascherando il prodotto centrale del corpus; fix: scarto dei candidati con newline, piu' vendor di sicurezza aggiunti alla stoplist. (c) **Fuga reale**: ragione sociale e hostname sono usciti nel repo pubblico in 14 punti attraverso i *nomi dei file*, per due percorsi indipendenti — la riga `- **Source**:` scritta verbatim senza `apply_anon`, e il preview del corpo che partiva dal frontmatter di tracciabilita', il quale ripete `source_file:` col nome del `.docx` originale. Fix su tre livelli: `enrich_graph.py` salta il frontmatter prima del preview (migliora anche la qualita' di ogni evidenza futura), `export_to_taxonomy.py` anonimizza il nome file, `sanitize_taxonomy_diff.py` ispeziona anche `source_file` e `text_preview` e li *scruba* con `[RIMOSSO]` invece di scartare, riportando il conto. Dopo il fix: 14 scrub, e ricerca delle stringhe sensibili nel diff a zero.

Metodo: la fuga e' emersa solo cercando le stringhe sensibili nel `git diff` del repo pubblico con l'`--apply` gia' fatto e nulla committato. Il riepilogo dell'export dichiarava 28 iniezioni e nessuna anomalia, e il `--dry-run` stampa le label e non i corpi. Ripristino con `git checkout -- docs/`, correzione, ri-esecuzione della catena da `enrich_graph`. Questo controllo e' ora scritto in `dev-testing.md` come passo obbligatorio.

Export finale: 28 iniezioni su 2 Capability (27 Cybersecurity, 1 Networking), `+267 / +10` linee, solo aggiunte. `mkdocs build --strict` verde. Commit skills-repo `5ca2dd6`. Ingest state track su `_Bitdefender (endpoint security)` soltanto: `_ GDPR E ISO27001` non tracciata deliberatamente, perche' ne sono stati processati 3 documenti su 13 e tracciarla la farebbe sparire dai digest a delta zero nascondendo i 10 non lavorati.

Due residui aperti, in `roadmap.md`. Il primo e' sistemico: `PSGSI Politica Sicurezza Informazioni` e' rimasta non classificata con punteggio *esattamente zero*, perche' il label e' italiano e le keyword sono inglesi, quindi zero token in comune a prescindere dalla semantica. Nessun intervento lessicale risolve questa classe, ed e' l'argomento piu' solido finora per gli embeddings. Il secondo e' di qualita': il preview e' un taglio a 200 caratteri che parte dall'H1 e finisce spesso dentro frammenti di tabella, difetto preesistente visibile anche nelle evidenze pubblicate il 16 luglio.

## 2026-07-27 — Automazione dell'inserimento nel diario e chiusura del debito (`6f3c2b8`)

Il diario aveva due mesi di ritardo e il motivo era il costo del passo manuale: aprire Word, incollare, riformattare. La regola in `CLAUDE.md` dichiarava il `.docx` intoccabile a programma per via della formattazione ricca. Verificato invece di assumere: il documento usa solo `Normal` e `Heading 1-4`, e le ventuno note a pie' di pagina hanno tutte lo stesso pattern XML. Test di round-trip su copia: python-docx salva senza perdere le trentaquattro tabelle, e la parte `word/footnotes.xml`, che tratta come blob opaco, si puo' riscrivere via `_blob` e persiste al salvataggio.

Nuovo `scripts/append_diary_section.py`: legge un draft Markdown nel formato che l'agente gia' produce e inserisce le sezioni prima di un paragrafo di ancoraggio (default `Lezioni apprese`), con `## C.N` -> Heading 2, `### x` -> Heading 3, `*x*`/`**x**`/`` `x` `` -> run corsivo/grassetto/monospazio, e `[^n]` piu' `[^n]: testo` -> note a pie' di pagina vere, con id allocati in continuita' sulle esistenti. Default in sola diagnosi, `--apply` scrive creando prima un `.bak` timestampato e rilegge il file confrontando il conteggio tabelle. La review vera resta il diff del `.md`: un inserimento sano produce solo aggiunte.

Primo uso reale: sezioni C.9-C.12 (anonimizzazione robusta e ciclo ARCHITETTURA, nav MkDocs a tre livelli e confine graphify, ciclo Cybersec, chiusura del debito con le due correzioni di oggi), diff del `.md` `+72 / -0`. Debito documentale dal 2026-05-28 chiuso.

Due difetti trovati dal test su copia e corretti prima di toccare il file vero: le note conservavano gli asterischi Markdown letterali (ora sono run formattati, acronimo in corsivo), e il draft conteneva quattro trattini lunghi vietati dalla regola tipografica. Un terzo sospetto, rimando a nota apparentemente spostato a fine paragrafo, era un errore di verifica: `p.text` non include i rimandi, e controllato sulla sequenza dei run il rimando risulta correttamente inline.

Corretti nello stesso giro due difetti del tooling esistente, entrambi emersi da una esecuzione reale. `finalize_diary.ps1` stampava i comandi git anche quando la rigenerazione non produceva modifiche, invitando a un commit vuoto con messaggio `Diario:`: ora controlla `git status --porcelain` e si ferma dicendo che il `.docx` non risulta modificato. `sync_diary_md.py` emetteva un `SyntaxWarning` perche' la docstring conteneva `.\.venv\Scripts\...` in stringa non raw. Aggiunto `*.bak.docx` a `.gitignore` e `.graphifyignore`. `CLAUDE.md` riscritto distinguendo la procedura automatica (nuove sezioni in coda) da quella manuale, che resta necessaria per tabelle, immagini, blocchi di codice formattati e modifiche a contenuto esistente.

## 2026-07-27 — Chiuse le due scoperte del ciclo Cybersec (`397c0a8` + skills-repo `88f4b8f`)

Sessione di manutenzione senza ingest. Tre blocchi di lavoro.

**Filtro nomi di graphify.** Il workaround manuale del 16 luglio diventa `scripts/prepare_graphify_source.py`. Replica `_SENSITIVE_DIRS` e `_SENSITIVE_PATTERNS` di graphify 0.8.14 (duplicati e non importati: graphify vive in un virtualenv pipx separato, e un cambio a monte deve rompere qui in modo visibile). Default in sola verifica, `--apply` genera `<nome>-sanitized/` con conversione `.docx` -> `.md` via `parse_docx.py`, nomi neutralizzati da una mappa esplicita (`password`->`autenticazione`, `credential`->`accesso`, `secret`->`riservato`, `token`->`chiave`, `private_key`->`chiave-privata`), frontmatter di tracciabilita' nel formato gia' usato dal ciclo Cybersec, H1 derivato dallo stem neutralizzato. Verifica di ritorno: rilegge l'output e rilancia il filtro, exit 2 se un nome attiva ancora il match. Materiale crittografico vero (per estensione) escluso e mai rinominato. Validato contro `_intermediate/src/Cybersec-governance-baseline-sanitized/`: i cinque nomi generati coincidono esattamente con quelli scelti a mano il 16 luglio. Casi limite verificati coerenti con graphify: `tokenizer.py` e `Passwordless-setup.docx` correttamente non scartati (lookahead alfanumerico).

**Routing GDPR.** Diagnosi precedente errata, vedi rettifica in coda alla voce del 2026-07-16. Causa reale duplice. (a) Bug in `generate_taxonomy_index.py`: `extract_capability_keywords` concatenava Technologies e Overview e tagliava a `keywords[:60]`, quindi su una pagina con sezione tecnica lunga il taglio azzerava il contributo dell'Overview invece di accorciare entrambe. Su `Cybersecurity & IT Governance` faceva sparire `gdpr`, `penetration`, `testing`, `malware`; cinque Capability su trentuno erano troncate. Fix: nuovo `_interleave_dedup` che fonde le due sezioni a giro con l'Overview in testa, piu' avviso su stderr con elenco delle pagine troncate e token persi. (b) Non un bug: il token decisivo era `breach`, presente nel vocabolario di `Quality Certification` e assente da quello di `Cybersecurity & IT Governance`, perche' la prima pagina dichiarava "GDPR data-breach handling" nell'Overview e "data breach register" nelle Technologies mentre la seconda non nominava mai il data breach. Decisione dell'utente: il perimetro GDPR/data breach appartiene a `Cybersecurity & IT Governance`. Pagine pubbliche riscritte di conseguenza (vocabolario di risposta al breach spostato su Overview+Technologies della pagina di sicurezza; su `Quality Certification` la rivendicazione resta solo in Responsibilities, che non alimenta le keyword, con rimando esplicito). Misura sul corpus del ciclo chiuso: automatico prima 18 a `Quality Certification` / 1 a `Cybersecurity`, dopo 0 / 20, cioe' l'automatismo riproduce da solo la correzione manuale di 18 fit. Il pubblicato era gia' corretto grazie al gate di review: cio' che si elimina e' la ripetizione della correzione a ogni ciclo. (c) Contraccolpo: il taglio equo faceva perdere a Cybersecurity `nessus`, `esxi`, `vcenter`. Sweep di `MAX_KEYWORDS` a 60/90/120/nessuno sullo stesso corpus: classificazione identica in tutti e quattro i casi, quindi il tetto non e' un parametro di tuning ma solo una guardia contro una pagina patologicamente lunga (`recall_score` normalizza sui token del nodo, non sulla cardinalita' del keyword set). Portato a 90; sul nav attuale tronca solo `System Administration`, di 4 token.

**Diario e documentazione.** Committati `open_diary.ps1` e `finalize_diary.ps1` (erano untracked), piu' i trigger di autopromemoria in `CLAUDE.md` e il passo 0 di pre-flight nella sequenza operativa. Prima esclusione per-subfolder in `.gitignore`/`.graphifyignore` (`_intermediate/src/Cybersec-governance-baseline/`). Draft del diario per le sezioni C.9-C.12 prodotto in scratchpad, copre il debito di due mesi dal 2026-05-28: aggiornamento del `.docx` ancora da fare.

Residui aperti, riportati in `roadmap.md`: l'ereditarieta' dei token della community in `classify_nodes` (spiega perche' l'errore riguardava 18 evidenze in blocco e non una), e `Soft Skills` che produce zero keyword e non puo' quindi ricevere evidenze. `mkdocs build --strict` sul repo pubblico: verde.

## 2026-07-16 — Ciclo Cybersec governance baseline (`5f2af1c`)

Primo ciclo di iniezione su un blocco di tre subfolder OneDrive coese (`Access Authentication` + `Procedura Data Breach + registro data breach + notifica al garante` + `Regolamento utilizzo sistemi informatici`), scelte come "test piccolo" del segmento Cybersec & IT Governance (447 file testuali totali della subfolder). Selezione manuale a 5 `.docx` policy/procedure generali, escludendo `Registro_accettazione.docx` (PII: firme di dipendenti), `Data Branch-bando.docx` (caso specifico), il duplicato piu' vecchio di `Configurazione-PC-Password.docx`, i `.pdf` (non processati dalla pipeline) e il `Registro_Data_Breach.xlsx` (PII garantite + non processato).

Due scoperte del ciclo, entrambe da riflettere in pipeline. Prima: **graphify 0.8.14 introduce `_SENSITIVE_PATTERNS` in `detect.py`** che skippa silenziosamente qualunque file con `password`, `secret`, `credential`, `token` nel nome (regex con lookaround su alfanum, quindi "auth" non e' toccato). Il pre-detect emette `total_files: 0` con `graphifyignore_patterns: 27` e la sessione graphify decide `needs_graph: false` senza mai eseguire l'analisi semantica, tacitamente. Nell'output diagnostico `skipped_sensitive` risulta pero' vuoto perche' la lista si popola solo nel loop `for p in all_files` a valle del pre-detect: quando il pre-detect abortisce sotto soglia, i file non arrivano mai fino al log. Workaround adottato in sessione: cartella parallela `_intermediate/src/Cybersec-governance-baseline-sanitized/` con file rinominati (`password` -> `autenticazione`) e convertiti `.docx` -> `.md`. Da valutare a pipeline: (a) prefisso di normalizzazione nome file lato `sanitize`/pre-graphify che rimappa i triggerword in placeholder neutri; (b) uso di `.graphifyinclude` per forzare inclusione mirata; (c) fork mirato di `_is_sensitive` per il caso lettore-doc, dove i nomi file possono contenere legittimamente termini di sicurezza. La direzione (a) sembra la piu' robusta perche' non dipende dal comportamento della skill.

Seconda: **`map_to_taxonomy.py` ha instradato 18 evidenze GDPR/Data Breach a `Quality Certification` (Management)** invece che a `Cybersecurity & IT Governance` (Security), perche' la keyword `certification`/`certifications` del vocabolario Quality Certification cattura come falsi positivi i nodi che citano "certificazione", "Regolamento", "WP250", "Provvedimento". Correzione applicata in sessione via edit chirurgico del `taxonomy_diff.sanitized.json` prima di export (spostamento capability slug + domain sui 18 fit interessati), non alla radice. Da valutare: rimuovere `certification`/`certifications` dalle keyword di Quality Certification (poco distintive) o alzarne il weight-down, e aggiungere `data breach`, `gdpr`, `dpo`, `garante`, `wp250`, `art. 33`, `art. 34` alle keyword di Cybersecurity & IT Governance per rendere il match diretto.

Numeri del ciclo. Graphify: 75 nodi, 102 link, 5 community. Enrich anonymization map: 5 voci (0 aziende, 0 IP, 0 hostname, 1 email pubblica `supporto@microsoft.com`, 4 PERSONA di cui 3 falsi positivi NER su testo di header `APPLICAZIONE\nIl` e simili, 1 vero-ruolo `Amministratore di Sistema`). Map to taxonomy: 36 fit / 1 new-cap / 5 new-domain / 25 non class. Sanitize gate: 34 fit kept (drop di `Amministratore di Sistema` [PERSONA_4] e `Area IT` per too-short-after-anon, 6 caratteri alfanum sotto la soglia 10). Review manuale: 18 fit spostate (`quality-certification` -> `cybersecurity-it-governance`), 4 fit misclassified rimosse (`gpedit.msc` -> Ad-hoc Internal Development, `Sistema di filtri categorie di siti` -> IT Administration & Billing, `Server aziendale / drive di rete condivisi` e `Backup dati` -> Cloud Platforms), tutti i new-domain e la new-cap scartati (score troppo basso, singolo nodo, community label rumorosa). Export apply: 30 iniezioni su 5 Capability, `+298 / -2` linee (le 2 rimozioni sono i placeholder "None yet" di due capability). Distribuzione finale: `Cybersecurity & IT Governance` 19, `Data Protection & Cryptography` 4, `Networking Engineering & Security` 4, `System Administration` 2, `Microsoft 365 Business` 1. Commit skills-repo `5f2af1c` (da `36987df`). Ingest state track su tre subfolder OneDrive, tutte a `5f2af1c`, delta zero.

**Rettifica 2026-07-27 alla seconda scoperta.** L'attribuzione del misrouting alla keyword `certification`/`certifications` di `Quality Certification` e' sbagliata e va considerata superata. Verificato sull'indice reale: `certification` compare nel vocabolario di *entrambe* le pagine e non ha deciso alcun match. Il token decisivo era `breach`, presente solo in `Quality Certification`; concorreva inoltre un troncamento a `keywords[:60]` in `generate_taxonomy_index.py` che eliminava `gdpr` proprio dal lato `Cybersecurity & IT Governance`. Anche il rimedio proposto allora era quindi mal indirizzato: togliere `certification` non avrebbe cambiato nulla. Diagnosi completa e correzione nella voce del 2026-07-27.

Nota metodologica: la selezione manuale di 5 file su un potenziale di 447 mostra il valore della strategia "segmento piccolo di test" scelta a inizio ciclo. La subfolder Cybersec ha diciotto subdir con contenuti eterogenei (crypto-at-rest, endpoint security, questionari fornitori, business continuity, ecc.); mischiarle in un unico grafo avrebbe prodotto community detection incoerente e review manuale ingestibile. La logica di segmentazione si basa su coesione semantica delle subdir, non su volume: le tre scelte formano insieme un blocco governance/policy compatto, non un campione random.

## 2026-07-15 — Bump schede a `233c39c`

Sync-context da `904f831` a `233c39c`. Due commit intermedi: `3c0dc29` (nuovo `.graphifyignore` in root) e `233c39c` (regola in `CLAUDE.md` che vincola l'allineamento manuale con `.gitignore`). Nessuna `covers-paths` cita esplicitamente questi file, ma `design-and-security.md` copre `.gitignore` e nella sezione "Separazione dei piani dati" tratta `_intermediate/` come gitignored per privacy: `.graphifyignore` e' la controparte per il piano graphify. Edit chirurgico su quella sezione della scheda per aggiungere una frase sul confine dedicato di graphify (indicizza `_intermediate/src/` sanitizzato, esclude venv/vault-output/.env/cache); aggiunto `.graphifyignore` ai `covers-paths` della scheda cosi' il prossimo drift lo intercetta direttamente. Bump di checkpoint sulle altre cinque schede (STACK, deployment, dev-testing, current-work, roadmap) senza edit di contenuto.

## 2026-07-15 — Aggiunto `.graphifyignore` e regola di allineamento

Committato `.graphifyignore` finora untracked in root: copia intenzionale di `.gitignore` senza `_intermediate/`, perche' graphify deve indicizzare i sorgenti sanitizzati in `_intermediate/src/<subfolder>/` che invece git esclude per privacy. Aggiunta in `CLAUDE.md` sezione "Regole operative" una nuova voce che vincola l'allineamento manuale delle due liste, dichiarando `_intermediate/` come unica differenza intenzionale. Il file era gia' operativo da tempo (usato nelle sessioni graphify) ma non versionato: il commit rende visibile il drift nel `git diff` quando una delle due liste viene toccata senza l'altra.

## 2026-07-15 — Bump schede a `904f831`

Sync-context da `bbf19bb` a `904f831`. Unico commit intermedio: `904f831` che estende `parse_nav()` in `scripts/generate_taxonomy_index.py` per supportare un nav MkDocs a tre livelli (Domain > SubArea > Capability) oltre al caso piatto a due livelli gia' supportato. Introduce due helper interni (`_collect_leaves` per la ricorsione sulle foglie, `_build_domain` per la costruzione del domain dict) e calcola `domain_dir` in stile POSIX per coerenza cross-OS. Il contratto verso il resto della pipeline non cambia: `taxonomy_index.json` mantiene la stessa struttura `domains[*].capabilities[*]`. Nessuna scheda cita esplicitamente la topologia a due livelli del nav come vincolo, quindi il salto e' un affinamento interno senza drift narrativo. Bump di checkpoint su tutte e sei le schede (STACK, design-and-security, deployment, dev-testing, current-work, roadmap) senza edit di contenuto. File untracked in root: `.graphifyignore` (fuori scope di questa sync).

## 2026-07-14 — Ciclo ARCHITETTURA + anonimizzazione robusta (`bbf19bb`)

Primo ciclo di iniezione su subfolder infrastrutturale (`ARCHITETTURA SERVER-CLOUD-LINEE`, 20 file testuali). Il dry-run iniziale sul grafo grezzo ha mostrato leak sistematici (IP interni, hostname, email aziendali, dominio, sede fisica) direttamente nei label dei nodi e nei preview: la catena esistente costruiva l'`anonymization_map` ma non la propagava fra `map_to_taxonomy` e `export_to_taxonomy`, e comunque copriva solo COMPANY/PROPER_NOUN. Refactor in tre punti: (a) `extract_entities.py` estrae ora anche `IP_ADDR` e `HOSTNAME` (regex con prefissi WIN/SRV/PC-/NAS/USG/VM e forma dashed uppercase, piu' stoplist), (b) `enrich_graph.py` estende la mappa con `[EMAIL_N]`, `[IP_N]`, `[HOSTNAME_N]`, (c) `map_to_taxonomy.py` propaga `anonymization_map` nel `taxonomy_diff.json`, (d) `export_to_taxonomy.py` applica la mappa al label H3, al name H1 di new-cap, al community label, oltre che al preview; rileva il line-ending nativo del file esistente (CRLF vs LF) e lo preserva in write, campiona il line-ending del docs-dir per i file nuovi; rigenera lo slug/file del new-cap dal name gia' anonimizzato. Nuovo script `sanitize_taxonomy_diff.py`: gate finale che scarta entries con contenuto insignificante dopo anon (min-chars alfanumerici) o con pattern residui non catturati dalla mappa (dominio intrawelt.*, "Intrawelt"/"Fastnet" nudi, `Punto Informatica`, `Vianova`, sede `Via Pescolla`/`Porto Sant'Elpidio`, ottetto IP abbreviato `(.NNN)`, `NAS INTRA*`/`NAS FTP`, `Ubuntu-NNNN-*`, applicativo `eGetrad`), e per new-capability filtra anche i nodi interni con residue. Workspace sanitizzato in `_intermediate/src/ARCHITETTURA SERVER-CLOUD-LINEE/` con selezione manuale (`.docx/.txt/.md` esclusi `USG20 (aborted)`, `USG60 (aborted)`, `SECUREPORTER`) — le sottocartelle escluse contenevano config firewall Zyxel e report SECUREPORTER identificati via grep credenziali/PSK/IP prima del run. Esito: 56 evidenze iniettate su 11 Capability page (`+551 / -6` linee, solo aggiunte + rimozione del placeholder "None yet"), 2 new-cap suggerite ma non pubblicate (titoli con placeholder residuo `[IP_7]` e semantica di basso livello: "Winsrv2019 Vm", "Windows Acl QTS" — non skill generali). Commit skills-repo `bbd361e`, `ingest_state track` ARCHITETTURA (20 file, delta zero). Sessione graphify eseguita su `account2` (nuovo parametro `-Account` del launcher); modello Italian NER `it_core_news_lg` installato nel venv del progetto (era assente, fallback regex era attivo). Tre commit lettore-doc: `ca55d90` (launcher `-Account`), `8976226` (anonimizzazione pipeline), `bbf19bb` (fix line-ending + slug + node filter).

## 2026-07-13 — Bump schede a `cb35334`

Sync-context da `ab89133` a `cb35334`. Nessun drift sui `covers-paths`: i due commit intermedi (`32a5920` aggiornamento templates, `cb35334` aggiunta skill `studio-didattico`) toccano solo `.claude/templates/**` e `.claude/skills/**`, aree non coperte da nessuna scheda. Bump di checkpoint su tutte e sei le schede senza edit di contenuto, per mantenere il prossimo confronto pulito.

## 2026-06-25 — Riconciliazione schede a `ab89133`

Sync-context da `979d674` a `ab89133`. Drift su quattro aree: `requirements.txt` (+`spacy>=3.7.0`, +`click>=8.0.0`), `.gitignore` (+`_notes/` per screenshot effimeri), `scripts/extract_entities.py` (NER `PROPER_NOUN` migrato a spaCy `it_core_news_lg` label `PER`, con fallback regex+stoplist). Edit chirurgico su `STACK.md` sezione "Deterministico prima del linguistico" per posizionare correttamente spaCy come modello statistico locale a zero token LLM, non come lavoro linguistico LLM. Le altre cinque schede ricevono solo bump a checkpoint: cambi marginali (`_notes/` in design/security; download spaCy gia' assorbito da setup; tuning permanente coerente con manutenzione corrente; nessun impatto su deployment e roadmap).

## 2026-06-17 — Adozione dello standard di progetto

Adottato il sistema portabile di contesto e documentazione: motore di riconciliazione, regole (`token-economy`, `interaction-style`, `git-identity-and-repo`, `manual-screenshots`), skill engine piu `onboard`, e catalogo pacchetti. Create le schede `context/` con frontmatter ancorato a `979d674`, riconciliate con `GUIDA-TECNICA.md` senza duplicarla, e la memoria iniziale. Attivati `code-context` (MCP, per mappare gli script Python) e `caveman` (uso selettivo, solo sessioni operative). `knowledge-wiki` non adottata: il progetto ha gia una knowledge base nativa (vault Obsidian piu graphify). Segreti verificati: `.env` mai committato. Skill custom `grafo-conoscenza`/`parsing-docx` e agent `lettore-documentazione` preservati.
