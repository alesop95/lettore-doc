# Work-log

> Append-only, ordine cronologico inverso.

## 2026-07-14 — Ciclo ARCHITETTURA + anonimizzazione robusta (`bbf19bb`)

Primo ciclo di iniezione su subfolder infrastrutturale (`ARCHITETTURA SERVER-CLOUD-LINEE`, 20 file testuali). Il dry-run iniziale sul grafo grezzo ha mostrato leak sistematici (IP interni, hostname, email aziendali, dominio, sede fisica) direttamente nei label dei nodi e nei preview: la catena esistente costruiva l'`anonymization_map` ma non la propagava fra `map_to_taxonomy` e `export_to_taxonomy`, e comunque copriva solo COMPANY/PROPER_NOUN. Refactor in tre punti: (a) `extract_entities.py` estrae ora anche `IP_ADDR` e `HOSTNAME` (regex con prefissi WIN/SRV/PC-/NAS/USG/VM e forma dashed uppercase, piu' stoplist), (b) `enrich_graph.py` estende la mappa con `[EMAIL_N]`, `[IP_N]`, `[HOSTNAME_N]`, (c) `map_to_taxonomy.py` propaga `anonymization_map` nel `taxonomy_diff.json`, (d) `export_to_taxonomy.py` applica la mappa al label H3, al name H1 di new-cap, al community label, oltre che al preview; rileva il line-ending nativo del file esistente (CRLF vs LF) e lo preserva in write, campiona il line-ending del docs-dir per i file nuovi; rigenera lo slug/file del new-cap dal name gia' anonimizzato. Nuovo script `sanitize_taxonomy_diff.py`: gate finale che scarta entries con contenuto insignificante dopo anon (min-chars alfanumerici) o con pattern residui non catturati dalla mappa (dominio intrawelt.*, "Intrawelt"/"Fastnet" nudi, `Punto Informatica`, `Vianova`, sede `Via Pescolla`/`Porto Sant'Elpidio`, ottetto IP abbreviato `(.NNN)`, `NAS INTRA*`/`NAS FTP`, `Ubuntu-NNNN-*`, applicativo `eGetrad`), e per new-capability filtra anche i nodi interni con residue. Workspace sanitizzato in `_intermediate/src/ARCHITETTURA SERVER-CLOUD-LINEE/` con selezione manuale (`.docx/.txt/.md` esclusi `USG20 (aborted)`, `USG60 (aborted)`, `SECUREPORTER`) — le sottocartelle escluse contenevano config firewall Zyxel e report SECUREPORTER identificati via grep credenziali/PSK/IP prima del run. Esito: 56 evidenze iniettate su 11 Capability page (`+551 / -6` linee, solo aggiunte + rimozione del placeholder "None yet"), 2 new-cap suggerite ma non pubblicate (titoli con placeholder residuo `[IP_7]` e semantica di basso livello: "Winsrv2019 Vm", "Windows Acl QTS" — non skill generali). Commit skills-repo `bbd361e`, `ingest_state track` ARCHITETTURA (20 file, delta zero). Sessione graphify eseguita su `account2` (nuovo parametro `-Account` del launcher); modello Italian NER `it_core_news_lg` installato nel venv del progetto (era assente, fallback regex era attivo). Tre commit lettore-doc: `ca55d90` (launcher `-Account`), `8976226` (anonimizzazione pipeline), `bbf19bb` (fix line-ending + slug + node filter).

## 2026-07-13 — Bump schede a `cb35334`

Sync-context da `ab89133` a `cb35334`. Nessun drift sui `covers-paths`: i due commit intermedi (`32a5920` aggiornamento templates, `cb35334` aggiunta skill `studio-didattico`) toccano solo `.claude/templates/**` e `.claude/skills/**`, aree non coperte da nessuna scheda. Bump di checkpoint su tutte e sei le schede senza edit di contenuto, per mantenere il prossimo confronto pulito.

## 2026-06-25 — Riconciliazione schede a `ab89133`

Sync-context da `979d674` a `ab89133`. Drift su quattro aree: `requirements.txt` (+`spacy>=3.7.0`, +`click>=8.0.0`), `.gitignore` (+`_notes/` per screenshot effimeri), `scripts/extract_entities.py` (NER `PROPER_NOUN` migrato a spaCy `it_core_news_lg` label `PER`, con fallback regex+stoplist). Edit chirurgico su `STACK.md` sezione "Deterministico prima del linguistico" per posizionare correttamente spaCy come modello statistico locale a zero token LLM, non come lavoro linguistico LLM. Le altre cinque schede ricevono solo bump a checkpoint: cambi marginali (`_notes/` in design/security; download spaCy gia' assorbito da setup; tuning permanente coerente con manutenzione corrente; nessun impatto su deployment e roadmap).

## 2026-06-17 — Adozione dello standard di progetto

Adottato il sistema portabile di contesto e documentazione: motore di riconciliazione, regole (`token-economy`, `interaction-style`, `git-identity-and-repo`, `manual-screenshots`), skill engine piu `onboard`, e catalogo pacchetti. Create le schede `context/` con frontmatter ancorato a `979d674`, riconciliate con `GUIDA-TECNICA.md` senza duplicarla, e la memoria iniziale. Attivati `code-context` (MCP, per mappare gli script Python) e `caveman` (uso selettivo, solo sessioni operative). `knowledge-wiki` non adottata: il progetto ha gia una knowledge base nativa (vault Obsidian piu graphify). Segreti verificati: `.env` mai committato. Skill custom `grafo-conoscenza`/`parsing-docx` e agent `lettore-documentazione` preservati.
