---
generated-from-commit: 979d674
generated-from-branch: main
generated-date: 2026-06-17
covers-paths:
  - scripts/**
  - run_pipeline.ps1
  - run_pipeline.sh
  - setup.ps1
  - setup.sh
last-verified-commit: fee90a6
source-doc: GUIDA-TECNICA.md
---

# Dev e testing

La pipeline e deterministica: rilanciare gli script da lo stesso output. Controlli chiave: `--dry-run` obbligatorio prima di `export_to_taxonomy.py --apply`; revisione manuale di `taxonomy_diff.md`; `sanitize_taxonomy_diff.py` come gate obbligatorio fra `map_to_taxonomy` e `export_to_taxonomy` (produce il `.sanitized.json` che poi va in dry-run/apply); `mkdocs build --strict` come safety net sui link del sito pubblico. La modalita `--incremental` confronta hash SHA256 e processa solo i file cambiati. Il tuning avviene su soglie e pesi (`THRESHOLD_FIT`, `W_JACCARD`, ecc.) documentati in `GUIDA-TECNICA.md` sezione 8. Ambiente ricreabile con `setup.ps1/.sh`; per l'estrazione NER serve `it_core_news_lg` scaricato via `python -m spacy download it_core_news_lg` (senza il modello si attiva un fallback regex+stoplist sub-ottimale). Sanity check post-`--apply` sullo skills-repo: `git diff -w --numstat` deve mostrare solo aggiunte (con eventuali `-2` legittimi dovuti alla rimozione del placeholder "None yet" dalle sezioni "Projects & evidence" preesistenti).

Il `--numstat` non basta pero' come controllo di riservatezza, e il ciclo Cybersec endpoint lo ha dimostrato: il riepilogo dell'export dichiarava ventotto iniezioni e nessuna anomalia mentre nel diff c'erano quattordici occorrenze della ragione sociale, arrivate li' dai nomi dei file. Il controllo che serve e' una ricerca esplicita delle stringhe sensibili nel `git diff` del repo pubblico, da fare con l'`--apply` gia' eseguito e nulla committato, cioe' nella finestra in cui `git checkout -- docs/` annulla tutto. Si cercano i cognomi noti, il dominio aziendale, la ragione sociale nuda, gli IP interni e gli hostname del parco macchine, e in parallelo si conta quante mascherature (`[PERSONA_N]`, `[EMAIL_N]`, `[IP_N]`, `[HOSTNAME_N]`, `[RIMOSSO]`) sono state effettivamente applicate: zero mascherature su un corpus aziendale e' piu' sospetto di molte. Il `--dry-run` non sostituisce questo passaggio, perche' stampa le label e non i corpi.

La revisione manuale del `taxonomy_diff.md` resta obbligatoria su tutto, ma non e' piu' cieca: `map_to_taxonomy.py` marca **DA VERIFICARE** i fit in cui la destinazione non e' determinata dal punteggio, e ne dichiara il motivo. Due criteri. Il margine sul secondo classificato entro `REVIEW_MARGIN` (0.05): a margine zero e' un pareggio, e vince la Capability che l'iterazione incontra prima, cioe' l'ordine del nav di MkDocs. Oppure la decisione poggiata su un solo token, che e' il caso di confidenza apparente alta ed evidenza minima. Per ogni fit marcato il diff riporta il secondo classificato col suo punteggio e i token che hanno deciso, il che rende la correzione diretta: sui pareggi il secondo classificato e' spesso la destinazione giusta. Validato sul ciclo endpoint: dieci fit marcati su trentuno, e tutti e cinque gli errori poi corretti a mano stavano fra quelli. Precisione cinquanta per cento, richiamo cento. Sul ciclo ARCHITETTURA i marcati sono trentaquattro su ottantacinque.

La riscrittura di evidenze gia' pubblicate con `--refresh` ha due controlli propri, oltre alla scansione delle stringhe sensibili. Il primo e' il conteggio degli ID di evidenza prima e dopo: una riscrittura sana ne conserva il numero, e se cala significa che `replace_block` ha divorato un blocco adiacente sbagliando i confini. Il secondo e' la presenza delle quattro H2 di contratto in ogni pagina toccata: sono il presupposto su cui l'export si appoggia, e una riscrittura che ne mangia una rompe silenziosamente le iniezioni future. Sul primo uso reale, la riparazione dei quarantaquattro preview del ciclo ARCHITETTURA piu' l'allineamento delle ventotto del ciclo endpoint, gli ID sono passati da 235 a 239 con quattro aggiunte attese e nessuna perdita, e tutte e undici le pagine hanno conservato le quattro H2. Da notare che in modalita' refresh il `--numstat` mostra legittimamente anche cancellazioni, quindi la regola "solo aggiunte" non vale e va sostituita da questi due controlli.

Un controllo nuovo, che gira da solo a ogni esecuzione dell'export, e' la passata dei collocamenti obsoleti: elenca i blocchi di evidenza appartenenti a nodi del corpus corrente che stanno su una pagina dove il diff non li colloca piu', distinguendo il nodo *spostato*, che il diff manda su un'altra Capability, dal nodo *non piu' previsto*, che e' sceso sotto soglia. Gira sempre, anche in dry-run e senza flag di rimozione, perche' e' l'unico controllo che vede questa classe di difetto: un'evidenza duplicata su due pagine non compare nel riepilogo delle iniezioni e non produce nessuna riga nel `--numstat`, quindi un controllo che va chiesto esplicitamente non verrebbe fatto proprio nelle sessioni in cui serve. La rimozione resta subordinata a `--prune-moved` o `--prune-unexpected`, e la lettura dell'elenco in dry-run precede sempre l'uso del secondo, che e' quello con cui una variazione di soglia cancellerebbe evidenze valide. Verificato su repository sintetico l'intero percorso, compreso il caso che conta di piu', cioe' che un diff i cui nodi sono estranei alle pagine pubblicate non rimuova nulla nemmeno con entrambi i flag attivi; sul repo pubblico reale la passata riporta zero collocamenti obsoleti su trenta evidenze.

Il controllo che conta piu' di tutti, dal 2026-08-03, e' `scripts/verify_public_repo.py`, e non e' opzionale: **nessun commit sul repository pubblico senza che sia uscito pulito**. Sostituisce la ricerca manuale delle stringhe sensibili, che era l'unico strato capace di vedere una classe di fughe e dipendeva dal ricordarsi di eseguirla e dal sapere cosa cercare, ed e' per questo che quattro password sono rimaste pubblicate per mesi. Gira sull'albero di lavoro per default, su ogni commit della storia con `--history`, e sul solo contenuto in stage con `--staged`, che e' la forma usata da `scripts/hooks/pre-commit`. L'hook si installa con `scripts/install_hooks.ps1` e va reinstallato dopo ogni clonazione del repository pubblico, perche' la cartella degli hook non e' versionata da nessuno. Per provarlo non serve un commit e non basta `git commit --dry-run`, che non esegue gli hook: si esegue l'hook a mano con il contenuto in stage.

Una regola di metodo emersa il 2026-07-29 e valida per qualunque modifica al gate di riservatezza o alla classificazione: non si considera fatta finche' non e' stata rilanciata sui corpora storici e confrontata con l'esito precedente. Il 2026-08-03 la regola ha avuto un'aggiunta pagata a caro prezzo: **sui corpora storici significa su tutti quelli disponibili**, non su un campione. La riconfigurazione di `soft/index.md` era stata misurata su due corpora con esito zero destinazioni cambiate, e sui due non misurati la pagina ha cominciato a ricevere funzioni PowerShell. Misurare su un sottoinsieme e' meglio che non misurare, e non equivale a misurare. I corpora disponibili si elencano cercando i `graph.json` sotto `_intermediate/src/` e nelle cartelle sorgente.

Terza aggiunta alla stessa regola: un miglioramento introdotto durante un intervento di emergenza va misurato come tutto il resto. L'abilitazione automatica delle Pages aggiunta al workflow durante la bonifica ha fatto fallire il primo deploy, perche' il token del workflow non ha i diritti per abilitare Pages dove non e' mai stato attivo; la scelta prudente e' stata tornare alla configurazione nota e funzionante. Il diff sintetico dice se la regola scatta, non cosa scarterebbe di buono. Applicata alla regola `residue-domain-third-party` ha corretto tre falsi positivi che il test sintetico non poteva mostrare, cioe' tre prodotti che le pagine dichiarano come tecnologia; applicata alla riconfigurazione di `soft/index.md` ha confermato che le sue novanta keyword non spostano nessuna destinazione. I due corpora di riferimento sono `_intermediate/enriched_graph.json` (endpoint, 31 fit) e `_intermediate/arch_enriched.json` (ARCHITETTURA, 85 fit), e il confronto si fa rigenerando l'indice della tassonomia in una copia di lavoro e mettendo a confronto le destinazioni per ID di nodo, non i totali, perche' due errori che si compensano tengono il totale fermo.

A monte della pipeline il controllo e' `prepare_graphify_source.py` in modalita' di sola verifica, che va eseguito su ogni subfolder prima di lanciare graphify. Il default e' non scrivere nulla: riporta quanti documenti passano il filtro, quanti verrebbero scartati per il nome, e per ciascuno il nome neutralizzato che verrebbe prodotto. Con `--apply` scrive la cartella e chiude con una verifica di ritorno che rilegge l'output e rilancia il filtro sui nomi appena scritti, uscendo con codice 2 se un file attiva ancora il match: la mappa di sostituzione non puo' quindi fallire in silenzio. Una singola conversione fallita non interrompe il ciclo, viene raccolta e riportata in coda. La validazione dello script e' stata fatta contro la cartella prodotta a mano nel ciclo Cybersec del 2026-07-16: i cinque nomi generati coincidono esattamente con quelli scelti manualmente allora.

Un secondo controllo riguarda `generate_taxonomy_index.py`, che avvisa su standard error quando una Capability supera `MAX_KEYWORDS` e viene troncata, elencando quali e di quanti token. Il troncamento silenzioso e' stato la causa reale di un misrouting, quindi il messaggio va letto: una Capability troncata perde vocabolario discriminante e viene battuta nel matching da pagine piu' povere che quel vocabolario conservano.

Il diario tecnico ha un controllo analogo, dove il rischio non e' la fuga di dati ma il disallineamento silenzioso fra il `.docx` sorgente e il `.md` derivato. `scripts/finalize_diary.ps1` e' il gate: fallisce presto se mancano il Python del venv, il convertitore o il `.docx`, propaga l'exit code di `sync_diary_md.py` invece di proseguire su una rigenerazione fallita, e mostra il diff del `.md` come unica review leggibile di un cambiamento nato in un binario. Se ne' il `.docx` ne' il `.md` risultano modificati si ferma dicendolo, senza stampare i comandi git: stamparli in quel caso invitava a un commit vuoto con messaggio `Diario:`, cioe' una voce di storia che dichiara un aggiornamento mai avvenuto. Lo stesso comando serve da verifica di allineamento a freddo, per esempio dopo un `git pull` su un'altra macchina: si rigenera e si guarda `git status`, e se il `.md` risulta modificato allora era il file su disco a essere disallineato. Il flag `-NoDiff` salta la sola review, non i controlli.

L'inserimento automatico delle sezioni con `append_diary_section.py` ha una verifica propria e una di sistema. Quella propria: il default e' la sola diagnosi, che riporta sezioni, paragrafi e note e segnala i rimandi senza definizione e le definizioni mai citate; l'`--apply` crea sempre un `.bak` timestampato prima di scrivere, e dopo il salvataggio rilegge il file confrontando il numero di tabelle prima e dopo, perche' una perdita di tabelle e' il modo tipico in cui una riscrittura programmatica di un `.docx` danneggia il documento. Quella di sistema, piu' importante, e' il diff del `.md` prodotto subito dopo da `finalize_diary.ps1`: un inserimento sano produce solo aggiunte, quindi qualunque cancellazione in quel diff significa che qualcosa di preesistente e' stato alterato e si ripristina dal backup. Sul primo uso reale, le quattro sezioni C.9-C.12, il diff e' risultato di settantadue righe aggiunte e zero rimosse. I `.bak` sono esclusi da `.gitignore` e `.graphifyignore` con il pattern `*.bak.docx`.
