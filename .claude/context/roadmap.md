---
generated-from-commit: 979d674
generated-from-branch: main
generated-date: 2026-06-17
covers-paths:
  - scripts/**
last-verified-commit: fee90a6
source-doc: GUIDA-TECNICA.md
---

# Roadmap

Estensioni candidate (da `GUIDA-TECNICA.md` sezione 9), da valutare caso per caso solo se il valore giustifica la dipendenza:

- Automazione multi-sorgente completa: oggi il passaggio graphify resta semi-interattivo; serve un flag headless o una API key dedicata.
- Embeddings semantici al posto del recall su keyword in `map_to_taxonomy.py`: cambia solo la funzione di scoring, il resto della pipeline resta invariato. Il ciclo Cybersec ha dato un argomento concreto a favore: con `recall_score` il match si decide su singoli token presenti o assenti dal testo di una pagina, quindi `breach` scritto in una Overview e non nell'altra sposta diciotto evidenze, e mantenere il routing corretto significa curare il vocabolario delle pagine invece della semantica.
- Token della community iniettati nei token del nodo in `classify_nodes`: oggi ogni nodo eredita i token del nome della propria community, quindi su un label da tre o quattro token la maggioranza del segnale non viene dal nodo. L'effetto e' che una community si sposta in blocco e un errore di routing non riguarda mai una evidenza sola. Da decidere se pesare i due contributi in modo diverso o rimuovere l'ereditarieta'; serve prima un ciclo in cui il fenomeno produca un errore osservabile e isolabile.
- Lingua del corpus contro lingua della tassonomia: `PSGSI Politica Sicurezza Informazioni` e' rimasta non classificata con punteggio *esattamente zero*, perche' il label e' italiano e le keyword delle Capability sono inglesi, quindi non esiste un solo token in comune con `security`, `policy` o `compliance`. Misurato il 2026-07-28 su due cicli: **un caso su 116 fit**, e i sei nodi a punteggio zero di ARCHITETTURA non sono casi di lingua ma modelli hardware specifici, materiale dismesso e prodotti SaaS che nessuna pagina nomina. Una prima stesura di questa voce lo dava come l'argomento piu' forte per gli embeddings: era una conclusione tratta da un singolo caso e va considerata sovrastimata. Il rimedio proporzionato e' un ponte bilingue, cioe' una mappa di termini IT italiani verso i corrispondenti inglesi applicata alla tokenizzazione, non un cambio di paradigma.
- Fragilita' del punteggio: **misurata e resa visibile il 2026-07-29, non risolta**. La scoperta e' che in otto fit su ventotto del ciclo endpoint il margine sul secondo classificato era *esattamente zero*, cioe' la destinazione veniva decisa dall'ordine di iterazione del nav di MkDocs e non dal punteggio. Tentato prima un pesaggio IDF dei token per capacita' discriminante: correggeva due delle sei assegnazioni note come sbagliate ma ne rompeva una che era giusta, quindi e' stato annullato, perche' un cambio che rimescola gli errori con guadagno netto di uno non giustifica di toccare una funzione di punteggio tarata. Tentato poi un ponte bilingue italiano-inglese sui token: peggiora, perche' porta il PSGSI da non classificato a instradato con sicurezza sulla pagina sbagliata, che e' peggio. Conclusione: con label di tre-sei token e match per sovrapposizione di insiemi il segnale e' troppo sottile perche' un aggiustamento lessicale riduca gli errori invece di spostarli. La strada resta una rappresentazione semantica; nel frattempo `map_to_taxonomy` marca i fit incerti e la revisione e' mirata (vedi `dev-testing.md`).
- Via di rimozione per un'evidenza pubblicata: **risolto il 2026-07-29**. `export_to_taxonomy.py` cerca a ogni esecuzione i collocamenti obsoleti e li rimuove con `--prune-moved` (nodo che il diff colloca su un'altra Capability) oppure `--prune-unexpected` (nodo conosciuto dal diff ma sceso sotto soglia). Il vincolo che rende sicura la rimozione e' che un collocamento si dichiara obsoleto solo per un nodo che il diff *conosce*: confrontare gli ID pubblicati con i soli ID attesi cancellerebbe le evidenze dei cicli precedenti, i cui nodi non compaiono in questo diff perche' venivano da un altro corpus. Con questo il prerequisito della riclassificazione retroattiva e' soddisfatto, quindi un cambio della funzione di punteggio applicato allo storico e' ora valutabile.
- Domini di terzi nel gate residue: **risolto il 2026-07-29** con la regola `residue-domain-third-party` in `sanitize_taxonomy_diff.py`, che riconosce un dominio registrabile e ha una allowlist per vendor tecnologici, fonti normative e namespace di codice con forma di dominio. Verificato che il buco fosse reale: la mappa di anonimizzazione non ha una categoria dominio, la categoria URL viene estratta ma non registrata, e nessuna delle regex hostname aggancia un dominio nudo minuscolo. Resta un falso negativo per costruzione, cioe' un dominio con suffisso fuori dalla lista corta di `KNOWN_TLDS`, da allargare solo se emerge dal corpus reale.
- Qualita' del preview delle evidenze: risolto il 2026-07-28 con `anchored_preview`, che sceglie il paragrafo con piu' token distintivi della label del nodo. Resta un margine: l'aggancio e' lessicale come il resto della pipeline, quindi su una label tradotta in inglese da graphify contro un documento italiano funziona solo per i token che sopravvivono alla traduzione (nomi di prodotto, sigle, numeri). E' la stessa radice del problema di lingua qui sopra, e si risolverebbe con lo stesso strumento.
- Tassonomia pubblica in tre lingue, italiano, inglese e spagnolo, intercambiabili, come gia' fatto per `E:\my-cv` e per il sito dei progetti. **Voce aperta il 2026-08-03, da progettare.** Non e' un lavoro editoriale soltanto: tocca la pipeline in profondita', perche' l'ID stabile di un'evidenza contiene lo slug della Capability e quindi cambierebbe per lingua, il contratto delle quattro H2 va deciso per ciascuna variante, e va stabilito in quale lingua l'export inietta le evidenze e come le altre due restano allineate. Da decidere prima di scrivere codice.
- Versioni delle action di GitHub su Node 24: le annotazioni del workflow segnalano che Node.js 20 e' deprecato sui runner. `checkout@v4`, `setup-python@v5`, `upload-artifact@v4` e `deploy-pages@v4` vanno aggiornati prima che il deploy si fermi da solo. Non urgente, non tocca la riservatezza.
- Avviso esplicito per una Capability a zero parole chiave in `generate_taxonomy_index.py`, sul modello di quello sul troncamento: oggi lo zero passa silenzioso, e questo ha contribuito a lasciare `Soft Skills` fuori dalla classificazione senza che nessuno lo notasse per settimane. Da distinguere nell'avviso il caso dichiarato in `MANUAL_ONLY_FILES` da quello accidentale.
- Capability senza keyword: **risolto il 2026-07-29** nella pagina pubblica e non nello script, e **rivisto il 2026-08-03**: la pagina e' ora in `MANUAL_ONLY_FILES` e non partecipa alla classificazione automatica, perche' con novanta parole chiave rubava evidenze tecniche sui corpora che la prima misura non aveva coperto. La causa non era un parsing rigido: `soft/index.md` non aveva nessuna delle quattro H2 di contratto, unica su trentuno, e il suo contenuto stava sotto cinque H2 tematiche; la foglia del nav si chiamava inoltre `Overview`, ed e' per questo che l'indice riportava una Capability con quel nome. Portata a contratto (skills-repo `cfeada5`) declassando le cinque intestazioni a H3 sotto le responsabilita', senza toccare la prosa. Misurato che le novanta keyword risultanti non rubino evidenze: zero destinazioni cambiate sui 31 fit del corpus endpoint e sugli 85 di ARCHITETTURA. Restano due voci minori: la pagina e' ora al taglio di `MAX_KEYWORDS` con sei token scartati, quindi e' la prima da guardare se emerge un misrouting, e `DOMAIN_BASE_KEYWORDS` non ha la chiave `Soft Skills`, cosa che non impedisce di ricevere evidenze ma impedisce al dominio di proporre nuove Capability. Resta aperta l'idea di un avviso esplicito per ogni Capability a zero keyword, sul modello di quello sul troncamento: oggi lo zero passa silenzioso.
- Connettori MCP per sorgenti remote (Google Drive, Notion, Confluence) che producono lo stesso formato `graph.json`.
- Clustering gerarchico nel vault al posto dell'approccio naive a singolo seed.
